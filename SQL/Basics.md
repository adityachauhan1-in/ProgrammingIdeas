# SQL is not case sensitive so you can write either lowercase or uppercase(preffered)
This is the first file of sql here i keep basic command of sql . 
# For creating the database 
CREATE DATABASE company; 

# To Check Database exist or not 
SHOW DATABASES;

# Select the database
USE database_name;
# A database is just the container. The actual structured data lives in tables.

CREATE TABLE table_name (
    
)
